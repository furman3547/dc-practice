const names=["John","James", "Kelly","Alfred"];

// for(let name in names){
//     console.log(names[i]); //all values in the arrray - not organized way of presenting data
    

//J=>John,James
//K=>Kelly
//A=>Alfred
const a=[];//syntax for declaring an empty array 
a[0]="Alfred"//assigning the first index in array to the name Alfred
const namesMap = new Map();//syntax for declaring a new map
namesMap.set("A","Alfred")// (KEY,VALUE) 
//if more than one name starting with A... use: 
    namesMap.set(A,["Alfred","Amstrong","Andy"])// Notice how there is an array for the VALUE for multiple names
namesMap.set("B","Bob")//(KEY,VALUE)

namesMap.get("A"); //looking for specific KEY A.. so you use a MAP instead of an Array to find by KEY
console.log(name)



