let n = [10,20,30,40,50]

function numArr (x, array)
    console.log (x," ",n)
    let numberfound=false;
    for(i=0,i<array.length;i++; // search the entire array, one at a time
        if(x==array[i]){
            console.log("Number found!")
        else{
                console.log("Number Not Found!")
            }
        }


        
        numArr(70,array)